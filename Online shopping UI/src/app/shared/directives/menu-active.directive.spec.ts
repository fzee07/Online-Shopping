import { MenuActiveDirective } from './menu-active.directive';

describe('MenuActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuActiveDirective();
    expect(directive).toBeTruthy();
  });
});
